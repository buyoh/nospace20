# tasks

## 課題

- CRLF / LF 混在
  - workflow で修正

## アプリケーションの課題

- 無限ループが発生した場合、テストが終了しなくなる。
- 構文エラーの位置のテスト
- whitespace の範囲でできない仕様があるかどうか？
  - 残しても良いが、それは extra な仕様であることを明示する必要がある。
  - ws モードと extra モードを用意する。ws モードは whitespace のみを扱い、extra モードは extra な仕様も扱う。

## 記号の意図・雰囲気

異なる意図で使われることを防ぐ

- `:` : 特殊な構文の識別子
- `;` : 文の区切り
- `,` : 配列・タプルの区切り。構文の区切り
- `.` : 小数点
- `(`, `)` : 関数呼び出し、式のグルーピング。let の初期化表現
- `[` , `]` : 識別子の直後なら配列アクセス、それ以外なら配列リテラル（未実装）
- `{`, `}` : ブロックスコープ式
- `#` : コメント
- `@` : 型注釈（未実装）
- `$` : 未使用
- `~` : 未使用（ビット反転？）
- `` ` `` : 未使用
- `%`, `&`, `^`, `|` : 演算子

基本の雰囲気

```
# 制御構文 #
identifier: expr1, expr2 { block };
# 宣言 #
identifier: name1[num](expr1), name2[num](expr2);
# 関数定義 #
identifier: name0(name1, name2) { block }
```

if がかなり特殊な構文であることに気づいた。

```
if: cond {block} else: if: cond {block} else: {block};
```

問題点

- func だけ `;` が無い
- `{}` は式なので、`identifier: {0}, {0} {block};` のように書ける。カンマが無い。
- 構文としてのブロックをやめたほうが良いかもしれない。代わりに`,`を入れる。`identifier: expr1, expr2, { block };`
  - `{}` を省略できるようになる
    - `if` は出来ない。`if: cond, eval else: eval;` のように、evalとelseの区切りが曖昧になるため。
      - `;` で区切る？
        - `if` 全体で式なので、式の終了と勘違いする
  - `,` の個数で判断すると、関数呼び出し内でのコンマ曖昧性が発生
  - `func` はいまいちな感じになる。そもそも式の`{}`と`func`の`{}`を区別しなくても良いようにできるか？


## 仕様の疑問点

### 名前空間

`{}` で記述されているが、これはスコープではない。
制御構文でもないし…

- `::` : `:` を識別子に使うので。
- `.`
- `->` : 関数
- `@` : 型に使いたい
- `$`

```
let: x(1);
namespace: MySpace {
  let: x(2);
  namespace: MySpace2 {
    let: x(3);
  }
  __clog(x);  # 2 を出力 #
  __clog(MySpace2.x);  # 3 を出力 #
}
```

ところで、chromiumスタイルのclang-formatは、`namespace` でインデントしない。

```
let: x(1);
NAMESPACE: MySpace;
let: x(2);
NAMESPACE: MySpace2;
let: x(3);
__clog(x);  # 2 を出力 #
__clog(MySpace2.x);  # 3 を出力 #
NAMESPACE_END: MySpace2;
NAMESPACE_END: MySpace;
```

### if 式。簡単だが、elseが無い場合の扱いは？
型を導入しなければ 0 を返せば ok。

```
let: x;
x = if: 1 {
  10;
} else: {
  20;
};
__clog(x);  # 10 を出力 #
```

## A 型 (未実装)

型システムは未実装。将来的に以下の型を導入予定。

- `int` : 整数
- `void` : 値なし
- `function` : 関数
- `tuple` : タプル

案2

```
func: add(a@int, b@int)@int {
  return: a + b;
}
```

## 多次元配列

```
let: arr[3][4]; # 3行4列の2次元配列を宣言 #
arr[0][0] = 1;  # 1 を代入 #
```

普通、`arr[y][x]` は `*(arr + y * W + x)` となるが、`W` をどのように管理するかが問題になる。
C 言語の場合、型システムから `W` を得る。
多次元配列を実装する場合は、型システムも同時に実装する必要がある。

## B 予約語（未実装）

未定義
予約すべき構文は全て`:`が末尾に来るため、予約語の衝突は起こりにくい。
それでも予約語が必要かどうか？

```
func: func(return) {
  let: let;
  let = return - 1;
  let: if(let > 0);
  if: if {
    return: func(let) + 1;
  } else: {
    return: 0;
  };
}
```

## C 関数ポインタ

- whitespace
  - 基本的に実装不能。スタックの値ではなく、特別な識別子を指定するため。なにこれ？
  - 同様の理由で、関数ポインタを引数に取る関数、無名関数も実装不能。
  - くそったれ案: 独自のidを付与する。idを見てlabelにジャンプするサブルーチンを生成する。デメリットは、実行時間がラベルの数に比例して増加すること。なんと、二分木構造にすれば、`log(n)` に抑えられる！

## D 外部ファイルの読み込み

`include: "filename.ns";` のような構文で、外部ファイルを読み込む機能。

- 文体について `INCLUDE:` にするとか。`#` は使えないので `#include` はダメ
- いつ評価する？
  - C言語風: トークン解析の段階で、`include` 文を見つけたら、指定されたファイルを読み込んで、その内容を現在のファイルの内容に挿入する。
  - モジュール: `include` 文を見つけたら、指定されたファイルをモジュールとして読み込む？

## E 一時配列

`puts("Hello")` と書きたい。

- ライフタイムはその文の評価中のみ。評価が終わったら破棄される。
- 例えば、`puts("Hello") + 3;` と書いた場合、`"Hello"` は一時配列として確保され、`puts` に渡される。`puts("Hello") + 3;` の評価が終わったら、`"Hello"` は破棄される。
  - 式ではない。式の場合、`"Hello"` が解釈された直後に破棄するので。

## G 便利な構文・糖衣構文

### G-1 repeat

while の糖衣構文と定義したいが、`continue` が呼び出されたときもインクリメントしたいため、`i = i + 1;` の配置位置が存在しない。

```
repeat: i(0), 5 {
  __clog(i);  # 0 1 2 3 4 を出力 #
};
```

そこで、 for を定義して、repeatは for の糖衣構文とする（解釈できるようにする）
省略は出来ないが、`{}`と書けば空になる

```
# 変数宣言、初期化、条件式、更新式、ループブロック #
for: i(0), i=0, i<5, i+=1 { __clog(i); };
```

構文変えたくなってきた。ブロックの前に`,`を入れたい。

変数宣言はletと同様の特殊な構文だが、以降は全て式なので、馴染み深い書き方は、

```
for: i(0), {}, i<5, i+=1, {
  __clog(i);
};
```

ただ、ifでrejectしたときと同様に、for, repeat をvoidを返す式として扱うと、関数呼び出しの中に含めることが可能になり、構文の区切り`,`が曖昧になる。
なので、式にすることを諦め、`return` `let` 等と同様に文として扱う。
`while` も式にする理由が無いので、文として扱うことにする。
スコープ式を使うことで、文を含めることができるため、表現力は十分である。

```
__puti__({let: x(0); repeat: i(0), 5, x += i; x;});
```

## H alias

```
alias: new_name(existing_name);
```

## I 無名スコープの修正

`{...}` と `(...)` が類似しているため、`{...}` を削除し、`(...)` に統合出来ないか検討する。
`{}` を別の用途に使えるようにするため。タプルとか。`:{}`で回避できなくもない

- `2 * (3 + 4)` のように使いたい。
- 一番簡単な方法としては、文と解釈すること。デメリットは、`2*(3+4;)` のように `;` が必要になること。
  - 最後の文は `;` を省略できるようにする（？）
- 式も文も両方許容する（ように実装できるか？）
  - `2 * (let: x(3); x + 4;)` のように、式の中で文も使えるようにする。
- 関数定義も while も `(...)` に？
  - `func: add(a, b) { return: a + b; }` → `func: add(a, b) ( return: a + b; )`

## 最適化・高速化

### 設計

- 意味解析の後に、意味解析最適化フェーズを追加する。
- 最適化の項目ごとに、プラグイン形式のような形で設計できるとよい。必要に応じて各最適化を有効化・無効化できるようにする。

### テスト・評価

これを評価する環境を作るのが先。

- 生成命令数（whitespaceの長さではない）
- 実行ステップ数

### 意味解析最適化

- 使っていない関数・変数の削除

### whitespace コンパイル最適化

しかし以下に列挙するものはいずれも「意味解析最適化」で実装する内容。
意味解析最適化以降でのみ使用される関数や構文を定義する。
ドキュメントも必要。

#### `__geti` / `__getiv` / `__getcv` の最適化

- `p = __geti()` はwhitespaceにとって余計な命令列を生成する。
- 過去には `__getiv(&p)`, `__getcv(&p)` が存在していた。
- 意味解析最適化以降でのみ使用される関数として、`__internal_getiv(&p)`, `__internal_getcv(&p)` を定義する。

#### `if`, `while` 文の最適化

- 条件付きgotoは、スタックの最上位がゼロの場合、 スタックの最上位が負の場合。
- nospaceの言語仕様のif文は、条件式がゼロでない場合に真とみなす。
- 次のような最適化が考えられる。`while` 文も同様。
  - 条件式が定数の場合、条件式を評価して、ブロックスコープに変換
  - 条件式が`== 0`の場合、特殊構文 `__if_zero` に変換。
  - 条件式が`< 0`の場合、特殊構文 `__if_negative` に変換。
  - 条件式が`>= 0`の場合、特殊構文 `__if_negative` を使うよう構造を変換。

#### 定数の畳み込み

- 定数式を評価して、定数に置き換える。

## Agent の強化

### 定型句っぽいの。スキル化したい

```
ソースコードについてユニットテストが十分か調査し、ドキュメントにまとめてください。
- あるモジュールに対してユニットテストを新たに作成しなければならないとき、追加は容易な設計になっているか？なっていないとき、どのような設計に置き換えなければならないか？
- ユニットテストの個数は十分か？
```

### タイムアウト指示はやっぱり要らないかも

`timeout` が bash とみなされるため、手動で許可ボタンを押さなければならない。

- 本ソフトウェアは軽量である。ライブラリアップデート・インストールを除き、15秒以上経過した場合は異常とみなす。`timeout` コマンドを使って 15 秒以上実行しないようにする
