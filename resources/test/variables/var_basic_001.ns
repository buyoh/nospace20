# 変数の基本: 定義と代入 #
func: main() {
  __trace(0);
  let:x;
  __assert_not(x);  # 初期値は0 #
  x = x + 3;
  __assert_not(x - 3);
  let:y;
  y = x * x;
  __assert_not(x - 3);
  __assert_not(y - 9);
  x = (y + 6) / x;
  __assert_not(x - 5);
}
