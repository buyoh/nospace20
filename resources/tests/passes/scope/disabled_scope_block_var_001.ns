# Block-scoped variable definition #
func: __main() {
  __trace(0);
  let:x;
  x = 1;
  if: 1 {
    # Define a new variable in block #
    let:y;
    y = 2;
    x = x + y;
  };
  __assert(x == 3);
  # y is out of scope #
}
