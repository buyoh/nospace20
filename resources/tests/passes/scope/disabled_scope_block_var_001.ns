# [未実装] ブロックスコープ内での変数定義 #
func: main() {
  __trace(0);
  let:x;
  x = 1;
  if: 1 {
    # ブロック内で新しい変数を定義 #
    let:y;
    y = 2;
    x = x + y;
  };
  __assert(x == 3);
  # __assert(y == 2);  # エラー: y はスコープ外 #
}
