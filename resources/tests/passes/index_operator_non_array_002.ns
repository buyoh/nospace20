# 非配列変数へのインデックス演算子書き込み #
func: main() {
  let: x;
  let: y;
  x = 100;
  y = 200;
  # x[0] = value は *(&x + 0) = value と同義 #
  x[0] = 111;
  __clog(x);
  # x[1] = value は *(&x + 1) = value でメモリ上の次の変数に書き込み #
  x[1] = 222;
  __clog(y);
}
