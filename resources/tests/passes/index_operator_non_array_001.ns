# 非配列変数へのインデックス演算子読み取り #
func: __main() {
  let: x;
  let: y;
  x = 100;
  y = 200;
  # x[0] は *(&x + 0) と同義で x の値を取得 #
  __puti(x[0]);
  __putc(10);
  # x[1] は *(&x + 1) でメモリ上の次の変数 (y) にアクセス #
  __puti(x[1]);
}
