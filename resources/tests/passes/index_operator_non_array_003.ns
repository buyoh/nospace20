# 非配列変数への & 演算子と [] の組み合わせ #
func: __main() {
  let: x;
  let: p;
  x = 123;
  # &x[0] は &x + 0 と同義で x のアドレスを取得 #
  p = &x[0];
  __puti(*p);
  __putc(10);
  # &x[i] は &x + i と同義 #
  p = &x + 0;
  __puti(*p);
}
