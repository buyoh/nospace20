# ホイスティングと初期化の組み合わせ #
func: __main() {
  __trace(0);
  a = 5;            # ホイスティングにより let: より前でアクセス可能 #
  __assert_not(a - 5);
  let: a(3);        # 宣言位置で初期化式を実行 #
  __assert_not(a - 3);
  __trace(1);
}
