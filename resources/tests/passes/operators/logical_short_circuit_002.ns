# 論理演算子: 短絡評価 (OR) #
# 左辺が非0ならば右辺は評価されない #

func: side_effect() {
    __trace(2);
    return: 1;
}

func: __main() {
    let: a;
    # 左辺が1なので右辺は評価されない (side_effect は呼ばれない) #
    a = 1 || side_effect();
    __trace(0); __assert(a == 1);
    
    # 左辺が0なので右辺も評価される (side_effect が呼ばれて trace[2] がインクリメント) #
    a = 0 || side_effect();
    __trace(1); __assert(a == 1);
    
    return: 0;
}
